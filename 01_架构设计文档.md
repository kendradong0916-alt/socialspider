# 小红书数据智能采集 MVP - 技术架构设计文档

## 1. 系统概览

### 1.1 核心价值主张
- **零爬虫维护成本**：利用 Skyvern Cloud API 的视觉智能体，无需自研反爬技术
- **快速上市**：前端 + 后端架构 1 周内完成开发和部署
- **实时数据**：支持按需触发爬取，返回实时的小红书笔记数据
- **高鲁棒性**：自动处理验证码、弹窗、UI 变化

### 1.2 技术栈选择
| 层级 | 技术方案 | 原因 |
|------|---------|------|
| **前端** | Next.js 14 + React 18 + TailwindCSS | SSR 支持、API Routes 方便、部署简单 |
| **后端** | Node.js (Express 框架集成在 Next.js 中) | 快速开发、与前端同语言、Vercel 原生支持 |
| **数据库** | Supabase (PostgreSQL) | 免费额度足够 MVP、开箱即用、实时订阅支持 |
| **任务队列** | Vercel Cron + Supabase Functions | 无需维护独立服务器 |
| **网络爬取** | Skyvern Cloud API | 官方住宅代理、视觉大模型解析、自动反风控 |
| **认证** | NextAuth.js | OAuth 集成、会话管理 |
| **部署** | Vercel | 按需计费、自动扩展、CI/CD 集成 |

---

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户浏览器                             │
│  (Web UI: 输入关键词 → 选择爬取网站 → 查看结果)              │
└────────────┬────────────────────────────────────────────────┘
             │ HTTPS
             ↓
┌─────────────────────────────────────────────────────────────┐
│                    Vercel Edge + Node.js                     │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Next.js 14 App Router                               │   │
│  ├──────────────────────────────────────────────────────┤   │
│  │ /api/tasks/create          → 创建爬取任务            │   │
│  │ /api/tasks/status          → 查询任务状态            │   │
│  │ /api/results               → 获取爬取结果            │   │
│  │ /api/profile/login         → 初始化浏览器登录态      │   │
│  └──────────────────────────────────────────────────────┘   │
└────────────┬──────────┬──────────────┬──────────────────────┘
             │          │              │
    ┌────────▼──────┐   │      ┌───────▼──────────┐
    │ Skyvern API   │   │      │  Supabase API    │
    │ (网络爬取)     │   │      │  (数据持久化)     │
    │               │   │      │                  │
    │ - 浏览器会话  │   │      │ - Task 表        │
    │ - 住宅代理    │   │      │ - Result 表      │
    │ - 验证码求解  │   │      │ - User 表        │
    │ - 视觉识别    │   │      │ - Profile 表     │
    └───────────────┘   │      └──────────────────┘
                        │
            ┌───────────▼────────────┐
            │  Vercel Cron / Queue   │
            │  (定时任务、异步处理)   │
            └────────────────────────┘
```

### 2.2 数据流转过程

#### **场景 1：高频商业用户（月费 $99）**
```
用户输入 (关键词、爬取网站、搜索范围)
  ↓
[API] /api/tasks/create
  ↓ 创建 Task 记录 (status='pending')
  ↓
[Skyvern] 启动浏览器代理，执行爬取
  ↓ (2-5 秒后返回)
  ↓
[API] 保存结果到 Supabase
  ↓ (status='completed', results=[...])
  ↓
用户刷新页面或 WebSocket 更新，查看数据
```

#### **场景 2：低频验证用户（免费 Trial）**
```
用户注册 → OAuth 授权 → 免费额度 (5 次/月)
  ↓
触发爬取 (status='pending')
  ↓
[Skyvern] 异步执行
  ↓ (返回 task_id)
  ↓
用户轮询 /api/tasks/status?task_id=xxx
  ↓
数据准备好 → 显示结果 + 下载选项
```

---

## 3. 核心模块设计

### 3.1 浏览器会话管理 (Browser Profile)

**目的：**复用登录态，减少 Skyvern API 调用成本

```
初始化流程：
  1. 用户首次点击 [绑定小红书账号]
  2. 前端重定向到 /api/profile/init
  3. 后端调用 Skyvern API 启动交互式浏览器
  4. 用户在浏览器中扫码/输入账号密码登录小红书
  5. 后端轮询 Skyvern 检查登录完成
  6. 生成 Browser Profile ID，保存到 Supabase
  7. 后续所有爬取请求复用此 Profile ID

成本效益：
  - 首次登录：1x Skyvern API 调用 (成本: $0.10)
  - 后续爬取：每次 1x API 调用 (无额外登录成本)
  → 相比每次重新登录，月度成本 ↓ 80%
```

### 3.2 任务管理系统

**Supabase 表结构：**

```sql
-- Tasks 表：追踪爬取任务
CREATE TABLE tasks (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES auth.users,
  website TEXT,           -- 'xiaohongshu', 'douyin', 'weibo'
  keyword TEXT,
  status TEXT,            -- 'pending', 'running', 'completed', 'failed'
  skyvern_task_id TEXT,   -- Skyvern 返回的任务 ID
  created_at TIMESTAMP,
  completed_at TIMESTAMP,
  error_message TEXT
);

-- Results 表：存储爬取结果
CREATE TABLE results (
  id UUID PRIMARY KEY,
  task_id UUID REFERENCES tasks,
  data JSONB,             -- 标准化的笔记数据
  raw_html TEXT,          -- 可选：原始 HTML（用于调试）
  created_at TIMESTAMP
);

-- User Profiles 表：用户的浏览器会话
CREATE TABLE browser_profiles (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES auth.users,
  website TEXT,
  profile_id TEXT,        -- Skyvern Browser Profile ID
  created_at TIMESTAMP,
  last_used_at TIMESTAMP
);
```

### 3.3 数据标准化引擎

**目的：**不同网站的数据转换为统一格式

```typescript
// 标准化输出格式
interface NormalizedPost {
  id: string;                    // 平台唯一 ID
  title: string;
  content: string;
  author: {
    id: string;
    name: string;
    avatar_url: string;
  };
  metrics: {
    likes: number;
    comments: number;
    shares: number;
    views: number;
  };
  publish_time: ISO8601;
  url: string;
  images: string[];             // 图片 URL 列表
  tags: string[];
  platform: 'xiaohongshu' | 'douyin' | 'weibo';
}
```

**转换逻辑：**
- 小红书：从页面 DOM 或 JSON API 提取 → 标准化
- 抖音：从播放器元数据提取 → 标准化
- 微博：从 Sina API 响应提取 → 标准化

---

## 4. API 设计

### 4.1 关键端点

#### **1. 创建爬取任务**
```http
POST /api/tasks/create
Content-Type: application/json

{
  "website": "xiaohongshu",
  "keyword": "美妆推荐",
  "filters": {
    "sort_by": "latest",      // 最新、最热
    "date_range": "7d"        // 7d, 30d, all
  }
}

Response 200:
{
  "task_id": "task_xxx",
  "status": "pending",
  "estimated_wait": "2-5s"
}
```

#### **2. 查询任务状态**
```http
GET /api/tasks/status?task_id=task_xxx

Response 200:
{
  "task_id": "task_xxx",
  "status": "completed",
  "progress": 100,
  "result_count": 15,
  "completed_at": "2024-06-10T12:30:00Z"
}
```

#### **3. 获取爬取结果**
```http
GET /api/results?task_id=task_xxx&page=1&limit=10

Response 200:
{
  "total": 15,
  "data": [
    {
      "id": "note_123",
      "title": "夏季护肤 5 步",
      "content": "...",
      "metrics": {
        "likes": 1200,
        "comments": 45,
        "shares": 20,
        "views": 5600
      },
      "publish_time": "2024-06-09T10:30:00Z",
      "images": ["url1", "url2"],
      "platform": "xiaohongshu"
    }
  ]
}
```

#### **4. 初始化浏览器会话**
```http
POST /api/profile/init
Content-Type: application/json

{
  "website": "xiaohongshu"
}

Response 200:
{
  "status": "waiting_for_login",
  "browser_url": "https://skyvern-cloud.xyz/browser/session_xxx",
  "timeout": 300  // 5 分钟内完成登录
}

后续轮询：
GET /api/profile/status?session_id=session_xxx
→ 登录完成后返回 { "status": "completed", "profile_id": "prof_xxx" }
```

### 4.2 认证方案

```
Authorization: Bearer {JWT Token}
  - JWT 由 NextAuth.js 签发
  - 包含 user_id、email、quota 等信息
  - 有效期：7 天
```

---

## 5. 成本估算

### 5.1 Skyvern API 定价

| 操作 | 成本 | 说明 |
|------|------|------|
| 浏览器交互 (1 个任务) | $0.10-0.30 | 取决于复杂度 |
| 验证码求解 | +$0.05 | 按次数计费 |
| 代理 IP 轮换 | +$0.01 | 可选 |

### 5.2 月度成本分解 (100 用户, 500 次爬取)

| 项目 | 月成本 | 备注 |
|------|--------|------|
| Skyvern API | $50-100 | 平均 $0.10-0.20/任务 × 500 |
| Supabase | $25 | Pro 计划 |
| Vercel | $20 | 按需付费 |
| **总计** | **$95-145** | |

### 5.3 盈利模式

```
收入 (假设 100 个付费用户 @ $99/月)：
→ 月收入 $9,900

成本：
→ 基础设施：$120
→ 人工维护：$1,000 (1 人/月)
→ 分销/营销：$2,000

毛利率：≈ 65%
```

---

## 6. 安全与合规

### 6.1 访问控制

1. **用户认证**：OAuth (微信、Google)
2. **配额限制**：
   - 免费用户：5 次/月
   - 付费用户：无限制 (同时限制 QPS=10)
3. **IP 白名单**：Skyvern 住宅代理自动处理

### 6.2 数据隐私

- 爬取的公开数据不涉及个人隐私
- 不存储用户密码（Skyvern 负责管理）
- 数据加密存储在 Supabase 中

### 6.3 服务条款

- 明确声明：仅用于市场研究、学术研究，不得用于商业竞争
- 每个网站的爬取频率受 Skyvern 限制（自动反风控）

---

## 7. 部署与扩展

### 7.1 初期部署 (Week 1)

```
Day 1-2: 核心架构搭建
  - Next.js 项目初始化
  - Supabase 表设计
  - Skyvern API 集成

Day 3-4: 前端开发
  - 登录界面
  - 爬取表单
  - 结果展示组件

Day 5: 后端 API 开发
  - 任务管理接口
  - 数据标准化

Day 6: 测试 & 部署
  - 端到端测试
  - Vercel 部署
  - 灰度发布

Day 7: 上线 & 监控
  - 性能监控
  - 错误日志
  - 用户反馈收集
```

### 7.2 扩展方向 (Week 2+)

1. **增加支持的网站**：抖音、微博、小红书笔记评论区深度爬取
2. **高级功能**：
   - 定时爬取 (Cron)
   - AI 分析 (GPT-4 摘要关键信息)
   - 数据导出 (CSV、Excel)
   - Webhook 实时推送
3. **商业变现**：
   - API 付费套餐
   - 企业版本 (私有部署)

---

## 8. 风险与应对

| 风险 | 影响 | 应对方案 |
|------|------|---------|
| Skyvern API 限流 | 爬取延迟 ↑ | 增加队列、预置缓存 |
| 小红书 UI 大幅改版 | 爬取失败 | Skyvern 视觉大模型自动适应 |
| 用户频繁登出 | 浏览器会话失效 | 定期刷新会话，提示重新登录 |
| 服务条款变化 | 法律风险 | 及时更新 ToS，主动沟通用户 |

---

## 附录：开发清单

- [ ] Skyvern 账户开通 & API Key 申请
- [ ] Supabase 项目创建
- [ ] Vercel 项目配置
- [ ] NextAuth.js OAuth 集成
- [ ] 核心 API 路由开发
- [ ] 前端 UI 组件开发
- [ ] E2E 测试
- [ ] CI/CD 管道配置
- [ ] 性能优化 (缓存、CDN)
- [ ] 上线前安全审计
