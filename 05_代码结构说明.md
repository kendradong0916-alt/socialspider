# DataSpider MVP - 完整代码结构说明

---

## 项目结构

```
dataspider-mvp/
├── app/                          # Next.js App Router
│   ├── page.tsx                  # 主页面（采集工具）
│   ├── layout.tsx                # 根布局
│   ├── api/
│   │   ├── tasks/
│   │   │   ├── create/route.ts   # 创建采集任务
│   │   │   ├── status/route.ts   # 查询任务状态
│   │   │   └── list/route.ts     # 列出用户任务
│   │   ├── results/route.ts      # 获取采集结果
│   │   ├── export/route.ts       # 导出数据 (CSV/JSON)
│   │   ├── auth/
│   │   │   ├── me/route.ts       # 获取当前用户信息
│   │   │   └── [...nextauth]/route.ts # NextAuth 配置
│   │   └── profile/
│   │       ├── init/route.ts     # 初始化浏览器会话
│   │       └── status/route.ts   # 查询会话状态
│   ├── login/                    # 登录页面
│   ├── results/
│   │   └── [taskId]/page.tsx     # 结果展示页面
│   └── dashboard/                # 仪表板（可选）
│
├── lib/                          # 工具函数
│   ├── db.ts                     # Supabase 客户端
│   ├── auth.ts                   # 认证工具
│   ├── skyvern.ts                # Skyvern API 封装
│   ├── normalize.ts              # 数据标准化
│   └── types.ts                  # TypeScript 类型定义
│
├── components/                   # React 组件
│   ├── TaskForm.tsx              # 采集表单
│   ├── ResultsTable.tsx          # 结果表格
│   ├── ExportButton.tsx          # 导出按钮
│   ├── ProgressBar.tsx           # 进度条
│   └── NavBar.tsx                # 导航栏
│
├── styles/                       # 样式文件
│   ├── globals.css               # 全局样式
│   └── tailwind.config.js        # Tailwind 配置
│
├── scripts/                      # 工具脚本
│   ├── db-init.js                # 数据库初始化
│   └── seed-data.js              # 种子数据
│
├── public/                       # 静态资源
│   ├── logo.svg
│   └── favicon.ico
│
├── .env.local                    # 环境变量（本地）
├── .env.production               # 环境变量（生产）
├── next.config.js                # Next.js 配置
├── tailwind.config.js            # Tailwind CSS 配置
├── tsconfig.json                 # TypeScript 配置
├── package.json                  # NPM 依赖
├── README.md                     # 项目文档
└── vercel.json                   # Vercel 部署配置
```

---

## 核心文件说明

### 1. API 路由

#### `app/api/tasks/create/route.ts`
```typescript
POST /api/tasks/create
Content-Type: application/json

// 请求
{
  "website": "xiaohongshu",
  "keyword": "美妆推荐",
  "filters": {
    "sort_by": "latest",
    "date_range": "7d"
  }
}

// 响应 (201)
{
  "success": true,
  "task_id": "uuid",
  "skyvern_task_id": "skyvern_uuid",
  "status": "pending",
  "quota_remaining": 4
}
```

**核心逻辑**：
1. 验证用户权限
2. 检查配额是否足够
3. 调用 Skyvern API 创建任务
4. 保存任务信息到 Supabase
5. 启动后台轮询

#### `app/api/tasks/status/route.ts`
```typescript
GET /api/tasks/status?task_id=uuid

// 响应
{
  "task_id": "uuid",
  "status": "completed",
  "progress": 100,
  "result_count": 15
}
```

#### `app/api/results/route.ts`
```typescript
GET /api/results?task_id=uuid&page=1&limit=10

// 响应
{
  "total": 15,
  "data": [
    {
      "id": "note_123",
      "title": "...",
      "metrics": { "likes": 1200, ... },
      ...
    }
  ],
  "export_urls": {
    "csv": "/api/export?task_id=xxx&format=csv",
    "json": "/api/export?task_id=xxx&format=json"
  }
}
```

### 2. 数据库类型

#### Supabase 表结构

**users 表**：
```sql
{
  id: UUID,
  email: STRING,
  token: STRING (JWT),
  plan: STRING ('free' | 'basic' | 'pro' | 'enterprise'),
  quota_limit: INT,
  quota_used: INT,
  quota_reset_date: TIMESTAMP,
  created_at: TIMESTAMP
}
```

**tasks 表**：
```sql
{
  id: UUID,
  user_id: UUID,
  website: STRING,
  keyword: STRING,
  status: STRING ('pending' | 'running' | 'completed' | 'failed'),
  skyvern_task_id: STRING,
  filters: JSONB,
  error_message: STRING,
  created_at: TIMESTAMP,
  completed_at: TIMESTAMP
}
```

**results 表**：
```sql
{
  id: UUID,
  task_id: UUID,
  data: JSONB (标准化数据数组),
  raw_html: TEXT (可选),
  created_at: TIMESTAMP
}
```

### 3. Skyvern API 集成

#### 调用流程

```typescript
// 1. 创建任务
const response = await axios.post(
  `${SKYVERN_API_URL}/tasks`,
  {
    website: 'xiaohongshu',
    action: 'search',
    params: {
      keyword: 'beauty tips',
      filters: { sort_by: 'latest' }
    }
  },
  { headers: { 'Authorization': `Bearer ${SKYVERN_API_KEY}` } }
);

const taskId = response.data.id; // 获取 Skyvern 任务 ID

// 2. 轮询任务状态
async function pollTask(taskId) {
  for (let i = 0; i < 30; i++) {
    const status = await axios.get(
      `${SKYVERN_API_URL}/tasks/${taskId}`,
      { headers: { 'Authorization': `Bearer ${SKYVERN_API_KEY}` } }
    );
    
    if (status.data.status === 'completed') {
      return status.data.results; // 返回采集结果
    }
    
    await sleep(1000); // 等待 1 秒
  }
}

// 3. 数据标准化
function normalize(rawData) {
  return rawData.map(item => ({
    id: item.id,
    title: item.title,
    metrics: {
      likes: item.likes,
      comments: item.comments,
      ...
    },
    ...
  }));
}
```

### 4. 认证流程

使用 NextAuth.js 处理：

```typescript
// app/api/auth/[...nextauth]/route.ts
export const authOptions = {
  providers: [
    GoogleProvider({
      clientId: process.env.GOOGLE_CLIENT_ID,
      clientSecret: process.env.GOOGLE_CLIENT_SECRET,
    }),
    // 其他 OAuth 提供商...
  ],
  callbacks: {
    async jwt({ token, user }) {
      if (user) {
        token.id = user.id;
      }
      return token;
    },
  },
};
```

### 5. 前端主页面 (app/page.tsx)

**关键组件**：
- `TaskForm`：采集表单
- `QuotaBar`：配额进度条
- `TaskHistory`：任务历史表格
- `ExportButton`：导出按钮

**状态管理**：使用 React Hooks + SWR

```typescript
const { data: tasks } = useSWR('/api/tasks/list', fetcher);

const handleSubmit = async (e) => {
  const response = await axios.post('/api/tasks/create', {
    website,
    keyword,
  });
  // 重定向到结果页面
  router.push(`/results/${response.data.task_id}`);
};
```

---

## 快速开发指南

### 添加新网站支持

**Step 1**: 更新常量
```typescript
// app/page.tsx
const WEBSITES = [
  { id: 'xiaohongshu', name: '小红书' },
  { id: 'douyin', name: '抖音' },
  { id: 'weibo', name: '微博' },
  { id: 'instagram', name: 'Instagram' }, // 新增
];
```

**Step 2**: 配置 Skyvern 参数
```typescript
// lib/skyvern.ts
const websiteConfig = {
  xiaohongshu: { url: 'https://www.xiaohongshu.com', ... },
  instagram: { url: 'https://www.instagram.com', ... },
};
```

**Step 3**: 配置数据标准化
```typescript
// lib/normalize.ts
function normalizeInstagram(rawData) {
  return rawData.map(item => ({
    id: item.id,
    title: item.caption,
    // ... 映射字段
  }));
}
```

### 添加新 API 端点

**创建文件**: `app/api/new-endpoint/route.ts`

```typescript
import { NextRequest, NextResponse } from 'next/server';

export async function GET(request: NextRequest) {
  // 你的逻辑
  return NextResponse.json({ success: true });
}

export async function POST(request: NextRequest) {
  const body = await request.json();
  // 你的逻辑
  return NextResponse.json({ success: true }, { status: 201 });
}
```

### 修改 Tailwind 样式

所有样式都使用 Tailwind CSS 工具类。如需自定义：

```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: '#3B82F6',
      },
      fontFamily: {
        sans: ['Inter', 'system-ui'],
      },
    },
  },
};
```

---

## 环境变量详解

```env
# Supabase 配置
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJx...  # 客户端可见
SUPABASE_SERVICE_KEY=eyJx...           # 服务端私密

# Skyvern 配置
SKYVERN_API_KEY=sk_...
SKYVERN_API_URL=https://api.skyvern.com/v1

# NextAuth 配置
NEXTAUTH_SECRET=random_string_32_chars  # 用于加密 token
NEXTAUTH_URL=http://localhost:3000     # 回调 URL

# OAuth (可选)
GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxx

# 应用配置
NEXT_PUBLIC_APP_NAME=DataSpider
NEXT_PUBLIC_FREE_QUOTA=5
```

**安全性提示**：
- `NEXT_PUBLIC_*` 开头的变量会暴露到客户端，不要放敏感信息
- `.env.local` 和 `.env.*.local` 文件不要提交到 Git
- 在 `.gitignore` 中添加 `.env.local`

---

## 测试清单

### 单元测试

```bash
npm test
```

### 端到端测试

```bash
npm run e2e
```

### 手动测试场景

1. **首次用户流程**
   - [ ] 注册新账户
   - [ ] 完成 OAuth 授权
   - [ ] 获得 5 次免费配额
   - [ ] 尝试第一次采集

2. **采集功能**
   - [ ] 选择不同网站
   - [ ] 输入有效关键词
   - [ ] 任务从 pending → completed
   - [ ] 看到采集结果

3. **错误处理**
   - [ ] 输入空关键词，显示错误
   - [ ] 配额用尽，提示升级
   - [ ] 网络错误，显示重试按钮

4. **导出功能**
   - [ ] 导出 CSV 可在 Excel 打开
   - [ ] 导出 JSON 有效格式
   - [ ] 图片下载完整无缺

---

## 生产部署检查

### 安全检查
- [ ] 所有敏感信息在 `SUPABASE_SERVICE_KEY` 中
- [ ] API 路由验证用户权限
- [ ] SQL 注入防护 (Supabase 已内置)
- [ ] CORS 配置正确
- [ ] HTTPS 启用

### 性能检查
- [ ] First Contentful Paint < 2s
- [ ] Lighthouse 评分 > 80
- [ ] 数据库索引创建
- [ ] CDN 缓存启用

### 监控配置
- [ ] 错误追踪 (Sentry)
- [ ] 性能监控 (Vercel Analytics)
- [ ] 成本告警
- [ ] 日志聚合

---

## 故障排查

### 常见错误

| 错误 | 原因 | 解决 |
|------|------|------|
| 401 Unauthorized | Token 过期或无效 | 重新登录 |
| 429 Too Many Requests | 超过频率限制 | 等待后重试 |
| 500 Internal Server Error | 服务器错误 | 检查日志 |
| 采集超时 | Skyvern 响应慢 | 增加超时时间或重试 |

### 调试技巧

```bash
# 查看实时日志
vercel logs --follow

# 查看数据库数据
supabase-cli db query "SELECT * FROM tasks"

# 测试 API
curl -X POST http://localhost:3000/api/tasks/create \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"website":"xiaohongshu","keyword":"test"}'
```

---

## 下一步优化建议

### 短期 (Week 2-3)
- 添加定时采集功能
- 实现用户通知 (邮件)
- 优化数据库查询性能

### 中期 (Month 2-3)
- AI 数据分析模块
- Webhook 推送功能
- API 接口文档

### 长期 (Month 4+)
- 国际化支持 (i18n)
- 企业级功能 (权限管理)
- 白标解决方案

---

**文档版本**：1.0  
**最后更新**：2024-06-10
