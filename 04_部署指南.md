# DataSpider MVP - 部署指南

**快速启动时间：30 分钟**

---

## 1. 前置条件

- Node.js 18+ (检查版本: `node -v`)
- npm 或 yarn
- Git
- 有效的 Skyvern API Key
- Supabase 账户
- Vercel 账户

---

## 2. 步骤 1: 准备环境

### 2.1 克隆代码

```bash
# 如果是 GitHub
git clone https://github.com/your-username/dataspider-mvp.git
cd dataspider-mvp

# 或者从本地项目启动
cd /path/to/dataspider-mvp
npm install
```

### 2.2 获取 Skyvern API Key

1. 访问 [https://www.skyvern.ai](https://www.skyvern.ai)
2. 注册账户 → 获得 API Key
3. 保存 API Key，后面会用到

### 2.3 创建 Supabase 项目

1. 访问 [https://supabase.com](https://supabase.com)
2. 登录 → 创建新项目
3. 选择区域（推荐：东京或新加坡，距离亚洲最近）
4. 等待项目创建（2-3 分钟）
5. 进入项目 → 点击 "SQL Editor"
6. 粘贴 `code_db_init.sql` 的内容，执行（创建所有表）
7. 回到项目首页，复制：
   - `Project URL` (即 NEXT_PUBLIC_SUPABASE_URL)
   - `anon key` (即 NEXT_PUBLIC_SUPABASE_ANON_KEY)
   - `service_role key` (即 SUPABASE_SERVICE_KEY)

---

## 3. 步骤 2: 本地配置

### 3.1 复制环境变量模板

```bash
cp code_.env.local.example .env.local
```

### 3.2 编辑 `.env.local`

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_KEY=your_service_key_here

# Skyvern
SKYVERN_API_KEY=your_skyvern_api_key_here
SKYVERN_API_URL=https://api.skyvern.com/v1

# NextAuth
NEXTAUTH_SECRET=your-random-secret-key-here
NEXTAUTH_URL=http://localhost:3000

# 其他配置
NEXT_PUBLIC_APP_NAME=DataSpider
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

**如何生成 NEXTAUTH_SECRET?**
```bash
openssl rand -base64 32
```

### 3.3 本地测试

```bash
npm run dev
```

访问 http://localhost:3000，应该能看到首页。

---

## 4. 步骤 3: 部署到 Vercel

### 4.1 推送到 GitHub (必须)

```bash
git init
git add .
git commit -m "Initial commit: DataSpider MVP"
git branch -M main
git remote add origin https://github.com/your-username/dataspider-mvp.git
git push -u origin main
```

### 4.2 在 Vercel 部署

#### **方法 A: Vercel Web 界面（最简单）**

1. 访问 [https://vercel.com](https://vercel.com) → 登录
2. 点击 "New Project"
3. 选择你的 GitHub 仓库 "dataspider-mvp"
4. **Environment Variables** 页面，添加所有 `.env.local` 中的变量：
   ```
   NEXT_PUBLIC_SUPABASE_URL
   NEXT_PUBLIC_SUPABASE_ANON_KEY
   SUPABASE_SERVICE_KEY
   SKYVERN_API_KEY
   SKYVERN_API_URL
   NEXTAUTH_SECRET
   NEXTAUTH_URL (重要：改为 https://your-vercel-domain.vercel.app)
   ```
5. 点击 "Deploy"
6. 等待部署完成（3-5 分钟）

#### **方法 B: Vercel CLI**

```bash
# 安装 Vercel CLI
npm install -g vercel

# 登录
vercel login

# 部署
vercel --env-file=.env.local

# 设置生产环境
vercel env add NEXTAUTH_URL
# 输入：https://your-domain.vercel.app
```

### 4.3 验证部署成功

1. 访问 Vercel 给你的域名 (如 `dataspider-mvp.vercel.app`)
2. 应该能看到首页
3. 尝试创建一个采集任务测试

---

## 5. 步骤 4: 配置自定义域名（可选）

### 5.1 购买域名

推荐平台：Namecheap、GoDaddy、阿里云

### 5.2 在 Vercel 中添加域名

1. Vercel 项目 → Settings → Domains
2. 输入你的域名
3. 按照提示添加 DNS 记录
4. 等待 DNS 生效 (5-48 小时)

---

## 6. 步骤 5: 功能验证清单

部署后，逐一检查以下功能：

- [ ] **注册/登录**：能否通过 OAuth 登录
- [ ] **创建任务**：能否成功提交采集请求
- [ ] **查看进度**：任务状态是否从 pending → completed
- [ ] **导出数据**：能否导出 CSV/JSON
- [ ] **错误处理**：输入空关键词时是否显示错误
- [ ] **性能**：首页加载时间 < 3s
- [ ] **日志**：错误是否在 Vercel 日志中显示

### 检查日志

```bash
vercel logs --follow
```

---

## 7. 常见问题 & 故障排查

### Q1: 部署后登录失败

**原因**：NEXTAUTH_URL 配置错误

**解决**：
```env
# 改为
NEXTAUTH_URL=https://your-domain.vercel.app
```

### Q2: Skyvern API 返回 401 错误

**原因**：API Key 过期或无效

**解决**：
1. 重新登录 Skyvern 获取新 Key
2. 更新 Vercel 环境变量

### Q3: Supabase 连接超时

**原因**：IP 白名单或网络问题

**解决**：
1. Supabase Settings → Database → 禁用 IP 限制（开发阶段）
2. 或添加 Vercel IP 到白名单

### Q4: 采集任务一直卡在 pending

**原因**：后台轮询失败，可能需要使用队列服务

**解决**：
```bash
# 查看日志
vercel logs -f

# 检查是否有错误信息
```

---

## 8. 性能优化

### 8.1 启用 CDN 缓存

Vercel 默认启用，无需配置。

### 8.2 数据库连接优化

```typescript
// app/lib/db.ts
import { createClient } from '@supabase/supabase-js';

// 使用连接池
const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
  {
    auth: {
      persistSession: true,
    },
  }
);

export default supabase;
```

### 8.3 监控成本

定期检查 Vercel & Supabase 的使用量：
- Vercel Serverless Function 执行时间
- Supabase 数据库查询次数
- Skyvern API 调用成本

---

## 9. 上线前检查清单

```
部署前：
- [ ] 所有环境变量已设置
- [ ] 数据库迁移已完成
- [ ] 单元测试通过
- [ ] 端到端测试通过

部署后：
- [ ] 首页能正常加载
- [ ] 采集任务成功创建
- [ ] 结果能正常显示
- [ ] 导出功能可用
- [ ] 错误提示清晰
- [ ] 性能满足要求 (FCP < 2s)
- [ ] 安全检查通过 (无 XSS/CSRF)

监控：
- [ ] 错误日志监控已配置
- [ ] 性能指标监控已配置
- [ ] 成本告警已设置
```

---

## 10. 后续运维

### 10.1 日常监控

```bash
# 查看实时日志
vercel logs --follow

# 检查部署状态
vercel status
```

### 10.2 定期更新

```bash
# 更新依赖
npm update

# 推送更新
git commit -m "chore: update dependencies"
git push

# Vercel 会自动重新部署
```

### 10.3 成本管理

- **Vercel**：每月 $0-20（按需付费）
- **Supabase**：每月 $25 Pro 计划
- **Skyvern**：每月 $50-200（按 API 调用量）
- **总成本**：约 $75-245/月

---

## 11. 下一步

部署成功后，可以考虑：

1. **添加更多平台**
   - 在 `WEBSITES` 数组中添加新平台
   - 在 Skyvern 配置相应的网站参数

2. **实现支付功能**
   - 集成 Stripe 或支付宝
   - 实现订阅管理

3. **增加高级功能**
   - 定时采集 (Cron)
   - AI 数据分析
   - Webhook 推送

4. **扩展营销**
   - Product Hunt 发布
   - 内容营销
   - KOL 合作

---

## 12. 获取帮助

- **Vercel 文档**：https://vercel.com/docs
- **Supabase 文档**：https://supabase.com/docs
- **Skyvern API**：https://docs.skyvern.ai
- **Next.js 文档**：https://nextjs.org/docs

---

**祝你部署顺利！如有问题，欢迎反馈。** 🚀
